# 复制此文件为 .env 并根据需要修改

# API 配置
NEXT_PUBLIC_API_DOMAIN=http://localhost:8010   # 局域网访问时改为 http://<本机 IP>:8010
NEXT_PUBLIC_WS_DOMAIN=ws://localhost:8010      # 局域网访问时改为 ws://<本机 IP>:8010

# 前端服务器配置
NEXT_HOST=0.0.0.0    # 监听地址：0.0.0.0=所有网卡（支持局域网访问），127.0.0.1=仅本地
NEXT_PORT=3010       # 前端服务端口
